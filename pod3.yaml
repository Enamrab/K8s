apiVersion: v1
kind: Pod
metadata: 
  name: nginx-pod
  labels:
    app: nginx
spec:
  restartPolicy: Never
  containers: 
    - name: nginx-cont
      image: nginx:latest
      ports:
        - containerPort: 80
