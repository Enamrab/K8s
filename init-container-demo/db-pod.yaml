apiVersion: v1
kind: Pod
metadata:
  name: db-pod
  labels:
    app: my-app  # This MUST match the service selector
spec:
  containers:
  - name: postgres
    image: postgres:15
    env:
    - name: POSTGRES_PASSWORD
      value: "mysecretpassword"
    ports:
    - containerPort: 5432

