apiVersion: v1
kind: ResourceQuota
metadata:
  name: resourcequota
  namespace: enam
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "5"
    limits.memory: 10Gi
    pods: "5"

